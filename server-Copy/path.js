import path from "path";
 export const STORAGE_PATH = path.resolve(import.meta.dirname, "storage");
 export const TRASH_PATH = path.resolve(import.meta.dirname, "trash");
 export const TempStoragePath=path.join(import.meta.dirname,"storage2")